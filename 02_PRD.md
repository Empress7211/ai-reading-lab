# 02｜产品需求文档（PRD）

## 1. 文档信息

- 产品：PaperWeave（工作名）
- 版本：v0.1
- 首发平台：桌面端，macOS 优先；架构兼容 Windows/Linux
- 首发学科：计算机科学 / 机器学习 / 算法研究
- 用户模式：单用户、本地优先、用户自带 LLM API
- 依赖：Zotero（优先本地 API）、本地 Git；GitHub 为首个远端适配器

## 2. MVP 目标

用户能从一个主题出发，完成以下闭环：

1. 生成一个有依据、有角色、有顺序的平衡论文包；
2. 合法获取或导入 PDF，并创建/匹配 Zotero 条目；
3. 在 PaperWeave 阅读器中阅读并留下稳定证据锚点；
4. 由 AI 分阶段生成笔记草稿，用户逐条审阅；
5. 形成一份结构化论文笔记与一个跨论文主题视图；
6. 将精简笔记写回 Zotero，将完整 Markdown 提交到本地 Git/可选 GitHub。

## 3. MVP 非目标

- 多人实时协作；
- 论文写作、投稿、同行评审工作流；
- 移动端完整编辑；
- 全自动系统综述或 PRISMA 工作流；
- 机构订阅代理；
- 付费全文抓取；
- 自动运行论文代码或实验；
- 双向、任意冲突下的 Git/Zotero 笔记实时同步。

## 4. 核心用户旅程

### Journey A｜冷启动进入新主题

1. 首次启动，选择研究领域、常用来源、默认语言；
2. 连接 Zotero；可选连接本地 Git 仓库和 GitHub；
3. 配置 LLM Provider：基础 URL、密钥、文本/视觉/Embedding 模型；
4. 输入“检索增强生成的评估方法”；
5. 选择阅读目标：快速入门 / 深度研究 / 复现评估；
6. 获得 8–12 篇平衡阅读包；
7. 查看每篇的角色、理由、先修关系、开放获取状态和预计阅读成本；
8. 选择第一篇，进入获取与阅读流程。

### Journey B｜读一篇论文并形成可信笔记

1. 系统检查 Zotero 是否已有条目/PDF；
2. 若没有，依次检查开放获取位置；无合法 PDF 时提示用户从机构或本地导入；
3. 进入阅读器，展示阅读前简报：为什么读、先看哪些章节、需要验证的主张；
4. 用户划线或选择图表，创建批注/问题；
5. AI 生成“待审阅”主张、方法卡、局限和关联；
6. 用户逐条接受、编辑或驳回；
7. 阅读结束，系统生成“认知变化、未解问题、下一步行动”；
8. 用户确认后同步 Zotero 与 Git。

### Journey C｜读完多篇后形成主题理解

1. 系统基于 Verified 笔记构建命题列表；
2. 合并同义命题，提示用户确认；
3. 生成支持/冲突/不可比矩阵；
4. 展示方法谱系、基准变化、证据缺口和未读关键论文；
5. 用户将其中一条洞见转化为研究问题、实验任务或新的阅读队列。

## 5. 功能需求

### EPIC A｜账户、初始化与本地工作区

#### FR-A01 首次初始化

系统应收集：

- 界面语言与笔记输出语言；
- 研究领域，可多选；
- 默认阅读模式；
- PDF/笔记本地目录；
- 是否允许匿名产品分析；
- 是否启用云元数据服务。

**验收：** 未连接任何外部服务时，用户仍可导入本地 PDF 并手工阅读、记笔记。

#### FR-A02 模型配置

支持 Provider Profile：

- 名称；
- OpenAI-compatible 或内置 Provider 类型；
- Base URL；
- API Key（首次保存后由 PaperWeave 本机配置持续使用；不进工作区快照、导出、日志或 Git，可在设置中替换或清除）；
- 文本模型、推理模型、视觉模型、Embedding 模型；
- 能力标记：JSON 输出、工具调用、视觉、最大上下文、并发限制；
- 每百万 Token 价格（用户可自定义，用于成本估算）；
- “测试连接”与最小权限检查。

**验收：** 用户可以为“分类、深度综合、图表理解、Embedding”分别选择模型；任何模型不可用时有可理解的降级说明。

#### FR-A03 本地工作区

- 使用 SQLite 保存领域数据；
- 所有对象具有稳定 UUID 和 schema_version；
- 支持导出完整工作区为 ZIP（不默认包含受版权保护 PDF）；
- 支持工作区备份和恢复；
- 工作区可在没有账户时使用。

### EPIC B｜主题发现与平衡阅读包

#### FR-B01 系统主题建议

首页展示 3–6 个主题：

- 基于用户手工选择的研究兴趣；
- 可选结合 Zotero 最近新增、最近阅读和收藏；
- 显示主题来源：用户兴趣 / 本地库趋势 / 新论文提醒；
- 提供“换一批”，但不能完全随机，应保持多样性和历史去重。

#### FR-B02 自定义主题

用户可输入关键词、自然语言问题、种子论文 DOI/标题，或选择 Zotero Collection。

可调参数：

- 阅读目标：入门、前沿、复现、争议、系统综述预研；
- 时间范围；
- 论文数量；
- 开放获取优先/仅 OA；
- 理论/实证/工程偏好；
- 新颖性—经典性滑杆；
- 是否强制包含反方视角；
- 排除作者、期刊、关键词或已读论文。

#### FR-B03 主题包生成

默认构成为：

- 基石 3 篇；
- 当前发展 3 篇；
- 反方视角 2 篇；
- 综述/桥梁 1 篇；
- 复现/数据集/基准 1 篇（可选）。

每篇必须显示：

- 被选中的角色；
- 1–3 条可审计理由；
- 角色置信度；
- 与种子/其他论文的关系；
- 年份、作者、来源、引用信息（如可用）；
- OA/PDF 状态；
- 用户 Zotero 中是否存在；
- 推荐阅读顺序；
- 预计阅读模式：略读/深读/查证。

#### FR-B04 主题包编辑

用户可：

- 替换单篇但保持角色；
- 将论文移到另一角色并记录用户修正；
- 固定论文后重新生成其余部分；
- 保存为长期追踪主题；
- 标记“不相关、过时、重复、角色错误、已读”。

#### FR-B05 推荐解释

系统不得只显示“AI 推荐”。解释必须来自以下可区分证据：

- 引用图结构；
- 语义相关度；
- 时间与引用增长；
- 多篇核心论文共同引用；
- 反方/批评语境；
- 用户库或阅读行为；
- LLM 对标题/摘要/引用上下文的角色判断。

### EPIC C｜论文详情与 PDF 获取

#### FR-C01 论文统一标识

优先级：DOI → arXiv ID → PMID/PMCID → Semantic Scholar/OpenAlex ID → 标题+第一作者+年份指纹。

系统需保存所有已知外部 ID，并支持合并重复记录。

#### FR-C02 PDF 获取链路

优先顺序：

1. Zotero 已存在的本地附件；
2. 用户本地指定文件；
3. arXiv/开放仓储的明确 PDF；
4. Unpaywall/OpenAlex/Semantic Scholar 返回的开放获取位置；
5. 出版者合法开放 PDF；
6. 无法自动获取时，打开 DOI/出版社/机构访问页面并等待用户导入。

要求：

- 不绕过登录、验证码、付费墙或 robots/访问控制；
- 记录 URL、获取时间、license、版本（submitted/accepted/published）和文件哈希；
- 下载前后校验内容类型与文件大小；
- 扫描文件按需 OCR，不重复 OCR 可选中文本 PDF；
- 默认不把 PDF 上传到产品云端。

#### FR-C03 写入 Zotero

创建或匹配 Zotero 顶层条目，补齐元数据，附加 PDF，并加入用户选择的 Collection。

默认标签：

- `paperweave/topic/<slug>`；
- `paperweave/role/foundation|frontier|counterpoint|bridge|resource`；
- `paperweave/status/queued|reading|verified|synthesized`。

### EPIC D｜阅读器

#### FR-D01 布局

桌面默认三栏：

- 左：目录、缩略图、搜索、主题包上下文；
- 中：PDF 页面；
- 右：AI 阅读台（Guide / Claims / Notes / Ask）；
- 可折叠为专注模式。

#### FR-D02 阅读状态

论文状态：

`queued → skimming → reading → review_pending → verified → synthesized`

用户可手动覆盖；系统记录进入状态和完成条件。

#### FR-D03 证据锚点

每个高亮、批注和 AI 提取块保存：

- PDF asset hash；
- page index；
- 文本选择；
- 四边形/矩形坐标；
- 前后文 hash；
- section heading；
- figure/table/equation ID（如解析到）；
- 创建来源和时间。

PDF 版本变化后，应尝试通过文本 hash 和邻近内容重定位，并标记失效锚点。

#### FR-D04 批注类型

- Highlight；
- Question；
- Claim；
- Evidence；
- Limitation；
- Idea；
- Reproduction task。

用户可自定义颜色，但语义类型不依赖颜色。

#### FR-D05 选区操作

选中文本后提供：

- 解释；
- 翻译；
- 加入主张；
- 标为证据/局限；
- 找到引用论文；
- 查找支持/反方论文；
- 添加个人笔记；
- 创建 GitHub/本地任务（后续可接 Issue）。

#### FR-D06 图表与公式

- 用户可框选图、表、公式；
- 有视觉模型时生成解释草稿；
- 必须保留页码与区域截图引用；
- 解释需区分“图中直接可见”“结合正文推断”；
- 无视觉模型时允许用户手工描述或只保存区域。

### EPIC E｜AI 笔记与审阅

#### FR-E01 阅读前简报

在完整解析后生成：

- 论文在主题包中的角色；
- 一句话研究问题；
- 先修概念；
- 建议阅读顺序；
- 3–5 个需要验证的问题；
- 可能的贡献与争议点；
- 与用户已读论文的差异。

所有事实性内容引用摘要/正文/元数据；仅基于标题摘要时必须提示。

#### FR-E02 证据账本（核心）

AI 生成的每个 Claim 包含：

- claim_text；
- claim_type：theoretical / empirical / methodological / interpretive；
- author_claim_or_ai_inference；
- evidence anchors；
- assumptions；
- scope_conditions；
- limitations；
- confidence；
- review_status；
- user_comment；
- related claims/papers。

#### FR-E03 方法卡

算法论文默认字段：

- 问题定义；
- 输入/输出；
- 核心方法与关键差异；
- 目标函数/训练策略；
- 数据集；
- Baseline；
- 指标；
- 主实验；
- 消融；
- 计算资源；
- 代码/模型/数据可用性；
- 可复现风险；
- 适用边界。

字段没有证据时留空，不允许补全常识。

#### FR-E04 AI 草稿审阅

每个笔记块具有：

- `draft`：AI 生成、未审阅；
- `accepted`：用户原样接受；
- `edited`：用户修改后接受；
- `rejected`：用户驳回并可选原因；
- `stale`：原文/PDF/模型变化后需复核。

批量接受默认关闭。用户可以启用“低风险字段批量接受”，但引用锚点和关键结论仍需抽查。

#### FR-E05 阅读结束卡

包含：

- 这篇论文真正改变了什么；
- 哪些结论我接受/暂不接受；
- 最大不确定性；
- 与已有知识的冲突；
- 可复用方法/实现点；
- 下一篇应读什么以及原因；
- 可转为实验或代码任务的行动项。

其中“我接受/不接受”默认只由用户填写；AI 可提供问题与候选，但不能替用户定稿。

#### FR-E06 跨论文综合

基于 Verified blocks 生成：

- 命题聚类；
- 支持/反方/不可比矩阵；
- 定义差异；
- 方法演化；
- 数据集/基准漂移；
- 证据强弱与复现实况；
- 研究空白；
- 新阅读候选。

### EPIC F｜Zotero、Git 与同步

#### FR-F01 权威来源划分

- Zotero：书目元数据、附件、Collection、精简笔记；
- PaperWeave SQLite：阅读状态、锚点、结构化 Claims、审阅历史；
- Git：完整 Markdown/JSON 快照与版本历史。

#### FR-F02 Zotero 同步

MVP 采用：

- 读：导入 Zotero 条目、附件、Collection、标签；
- 写：新增/更新条目、附件、标签、精简笔记；
- 不覆盖用户非 PaperWeave 创建的 Zotero 笔记；
- PaperWeave 创建的 Note 带稳定 marker 和反向链接；
- 冲突时创建新版本并提示，不静默覆盖。

#### FR-F03 Git 导出

默认结构：

```text
research-notes/
  papers/<citekey>/index.md
  papers/<citekey>/claims.json
  topics/<topic-slug>/index.md
  topics/<topic-slug>/reading-pack.json
  assets/                 # 默认不含 PDF，仅可选小型用户生成截图
  .paperweave/config.json
```

每次同步：

- 将结构化笔记渲染为稳定 Markdown；
- 提交到 `paperweave/<date-or-topic>` 分支；
- 默认不直接推 main；
- GitHub 连接后可创建 Pull Request；
- commit message 可读且可配置；
- PDF 永不默认提交。

#### FR-F04 冲突策略

MVP 支持单向导出＋有限回读：

- PaperWeave 写入有 machine marker 的区块；
- 用户可在 Markdown 的 `## My Notes` 区块自由编辑；
- 回读仅同步该区块和 frontmatter 的允许字段；
- 结构化 Claims 的外部修改需要显式导入审阅；
- 发生冲突时保留两份并展示 diff。

### EPIC G｜搜索、知识库与复习

#### FR-G01 全局搜索

可搜索：

- 论文标题、作者、摘要；
- Verified Claims；
- 用户笔记；
- 批注文本；
- 主题与标签；
- 数据集、指标、方法和代码仓库字段。

支持过滤：角色、阅读状态、时间、来源、是否有 PDF、是否有反方证据。

#### FR-G02 主题空间

每个主题空间包含：

- 当前阅读包；
- 已读/待读队列；
- 命题地图；
- 争议矩阵；
- 方法/时间线；
- 研究问题与行动项；
- 新论文监控（P1）。

#### FR-G03 可回顾性

系统应提供：

- “我当时为什么保存它”；
- “哪些判断后来被新论文修正”；
- “哪些未解问题仍未处理”；
- “哪些笔记锚点已失效或模型输出需复核”。

### EPIC H｜同步中心与可观测性

#### FR-H01 任务中心

显示：

- 元数据获取；
- PDF 下载/导入；
- PDF 解析；
- Embedding；
- AI 笔记生成；
- Zotero 写入；
- Git commit/push/PR；
- 错误、重试和取消。

#### FR-H02 成本与隐私提示

每个 AI 任务执行前/后显示：

- 发送了哪些数据（页范围、文本、图片）；
- 使用哪个 Provider/模型；
- 估算/实际 Token 与费用；
- 是否在本地执行；
- 日志中不得保存 API key 或完整敏感内容。

## 6. 权限与隐私

- Zotero 本地写入必须由用户显式授权；
- GitHub 仅请求选定仓库的 Contents 权限；
- 模型 API Key 只保存在 PaperWeave 本机配置中，首次保存后持续使用，并可在设置中替换或清除；
- 用户可为单篇论文关闭外部模型调用；
- 产品云端默认只接收查询、论文 ID 和非敏感反馈；
- 完整 PDF、批注与笔记上传必须单独 opt-in；
- 支持删除本地索引、撤销授权和导出数据。

## 7. 关键异常场景

| 场景 | 产品行为 |
|---|---|
| 没有 DOI | 使用其他 ID 或标题指纹，提示匹配置信度 |
| PDF 不是 OA | 不自动绕过；打开合法页面或等待本地导入 |
| Zotero 未启动 | 阅读仍可继续；写入任务排队，给出启动提示 |
| Zotero 版本不支持本地写 | 降级到 Web API、导出文件或可选插件方案 |
| PDF 解析失败 | 回退到基础文本层/页面图像；标记不可提取区域 |
| LLM 不支持 JSON | 使用文本解析＋schema repair，并降低可信等级 |
| 模型上下文不足 | 按章节分阶段处理，并禁止跨未读内容的确定性综合 |
| Git 冲突 | 不强推；创建冲突副本，展示 diff 和解决选项 |
| 论文内容包含提示注入 | 文档内容作为不可信数据，系统提示优先且禁用工具指令 |
| 推荐没有可靠反方论文 | 明确显示“未发现充分反方证据”，不硬凑角色 |

## 8. 性能与体验指标

- 首页本地数据可交互：启动后不依赖网络；
- PDF 首屏打开：本地文件应快速可见，深度解析异步进行；
- 高亮创建后即时显示，AI 处理不阻塞阅读；
- 所有后台任务可取消和重试；
- 主题包生成可先流式展示候选，再补全解释；
- 阅读器在常见笔记本分辨率下不出现横向滚动；
- 支持键盘操作与 `prefers-reduced-motion`。

## 9. MVP 发布门槛摘要

1. 真实 Zotero 本地读写链路通过兼容性测试；
2. OA PDF 获取不会绕过访问控制，且保存来源与 license；
3. 90% 以上可选中文本 PDF 的证据锚点可准确回跳；
4. AI 事实块必须有锚点或被标为 inference；
5. Git 导出可重复执行且不会产生无意义全文件 diff；
6. 用户可以完全离线阅读、手工记笔记和导出；
7. 所有模型调用可查看、取消并审计成本；
8. 原型中的发现—阅读—审阅—同步核心路径在产品中完整打通。
