# 08｜信息架构、交互与关键流程

## 1. 体验目标

PaperWeave 应让用户感到自己在进行研究，而不是在管理 AI 对话。界面要优先呈现：

- 当前研究问题；
- 论文在阅读路径中的角色；
- 原文与证据；
- 尚待用户判断的内容；
- 认知如何跨论文变化。

AI 对话框不是默认中心。任何生成结果都落到明确对象、位置和审阅状态。

## 2. 一级信息架构

```text
Discover      主题建议、搜索、主题包生成
Library       本地/Zotero 论文、队列、Collection、状态
Reading       最近阅读、待继续、阅读历史
Knowledge     Topic、命题、证据矩阵、问题与行动项
Sync          Zotero、Git、任务、冲突
Settings      模型、隐私、解析、快捷键、工作区
```

全局命令面板提供：打开论文、搜索知识、创建主题、跳转 Claim、执行同步。

## 3. Discover｜发现页

### 3.1 首屏

- 主输入：“你现在想理解什么问题？”；
- 可输入关键词、自然语言、DOI/标题、粘贴摘要；
- 当前上下文：领域、阅读目标、时间窗口；
- 3–6 个系统建议主题，显示建议来源；
- `换一批` 保持兴趣覆盖和历史去重，而非纯随机。

### 3.2 高级约束

折叠面板：

- 入门 / 深度研究 / 复现 / 争议；
- 经典—新颖滑杆；
- OA 仅限/优先；
- 强制反方；
- 理论/实证/工程；
- 论文数量；
- 排除项；
- 使用哪个 Zotero Collection 作为上下文。

### 3.3 生成状态

不要只显示 spinner。展示可理解步骤：

1. 解释主题；
2. 召回候选；
3. 合并版本；
4. 构建引用关系；
5. 寻找反方；
6. 排定阅读顺序。

用户可在中途取消；已有结果可逐步出现，但角色尚未稳定时标记“候选”。

## 4. Topic Pack｜平衡阅读包

### 4.1 默认视图

建议使用“阅读路径＋角色分区”的混合形式，而非三个孤立卡片列：

- 顶部：主题解释、范围、缺口与置信度；
- 中部：按推荐顺序的 Path；
- 侧边/筛选：Foundation / Frontier / Counterpoint / Bridge / Resource；
- 每篇 Paper Row 展示角色、理由、来源、OA、Zotero、预计阅读模式；
- 论文之间用 `prerequisite / extends / counters / bridges` 连接。

### 4.2 Paper Row

必须包含：

- 标题、作者、年份；
- 角色与置信度；
- “为什么在这里”最多 2 条首屏解释；
- 一个可展开的 Evidence Drawer，查看引用图/语义/引用语境依据；
- OA/Zotero/PDF 状态；
- 阅读模式与预计成本；
- 操作：开始阅读、加入队列、固定、替换、角色纠正、排除。

### 4.3 用户控制

- 固定某篇，重新生成其余；
- `换一篇反方` 而不是整包刷新；
- 拖拽阅读顺序；
- 将系统角色改为用户角色；
- 保存为“持续追踪”；
- 对范围假设进行内联编辑。

### 4.4 空缺诚实表达

没有可靠反方时显示：

> 尚未找到直接反证。以下内容是方法学批评、边界条件或替代解释，不等同于结论被推翻。

禁止为满足配额制造假反方。

## 5. Reader｜三栏阅读器

### 5.1 布局

```text
┌──────────────── Top bar ────────────────────────────────┐
│ 返回主题｜标题｜版本｜Zotero｜同步｜阅读进度｜更多       │
├────────────┬────────────────────────┬───────────────────┤
│ 左侧导航    │ PDF / Reflow 阅读区     │ 研究工作区         │
│ 目录        │                        │ 阅读导引            │
│ 缩略图      │ 原文、高亮、图表、公式   │ 证据账本            │
│ 文内搜索    │                        │ 我的笔记            │
│ 主题上下文  │                        │ 提问                │
└────────────┴────────────────────────┴───────────────────┘
```

侧栏均可收起；中间阅读区始终优先。

### 5.2 顶部状态

- 当前 PDF 版本（preprint/published）；
- Zotero 已连接/待写入；
- 本地保存状态；
- 待审阅数量；
- 模型调用中及成本；
- 不显示造成焦虑的伪精确“阅读完成百分比”，而以已访问关键章节和用户标记为准。

### 5.3 阅读导引 Tab

一屏内回答：

- 为什么读；
- 研究问题；
- 先修；
- 推荐章节路径；
- 需要验证的问题；
- 关键图表。

每一项可点击跳转；仅基于摘要时显示醒目标识。

### 5.4 证据账本 Tab

采用可扫描的原子卡：

```text
[作者主张｜Empirical｜Draft]
“在 X 设置中，方法相对 Y 提升 Z。”
证据：p.6 Table 2 · p.7 Ablation
范围：数据集 A / 指标 B
AI 置信度：0.81，依据：正文陈述＋表格
[接受] [编辑] [驳回] [查看原文]
```

行为：

- Hover/聚焦卡片时原文 Anchor 同步高亮；
- 点击 Anchor 跳到页并短暂闪烁；
- 批量接受仅适用于低风险描述字段，不适用于结论性 Claim；
- 驳回可选原因，反哺后续建议；
- 用户修改后显示“AI 起草，经用户编辑”，不可伪装纯 AI/纯用户。

### 5.5 我的笔记 Tab

用户拥有自由书写区，同时可插入结构对象：

- `/claim`；
- `/question`；
- `/method`；
- `/limitation`；
- `/action`；
- `@paper`、`@claim`、`@topic`；
- 拖入高亮自动附 Anchor。

AI 可以提出改写/结构化建议，但不得无提示覆盖原文。

### 5.6 提问 Tab

默认问题范围明确显示：

- 当前选择；
- 当前章节；
- 当前论文；
- 当前主题的 Verified 知识。

回答由段落和“证据引用带”组成。回答中每个事实段落至少一个可点击 Anchor；超出论文的背景知识明确标为外部知识并提供来源或提示不可验证。

### 5.7 选择文本的浮动菜单

- 高亮；
- 加入证据账本；
- 解释；
- 翻译；
- 提问；
- 对比已读论文；
- 创建用户笔记。

“加入证据账本”先创建 Anchor，再调用 AI，确保来源先于生成。

## 6. 阅读结束流程

用户点击“完成本次阅读”后，不立刻生成长摘要，而进入 4 步收束：

1. **清理审阅队列**：显示关键未处理项；
2. **证据检查**：无 Anchor、Anchor 失效、Claim 混合；
3. **认知卡**：用户回答“我现在相信什么、仍不相信什么、改变了什么”；
4. **输出与下一步**：Zotero/Git 预览、下一篇、实验/问题任务。

认知卡建议字段：

- Before reading, I expected…
- The strongest evidence was…
- I remain unconvinced because…
- This changes my view of topic X by…
- One testable next step is…

AI 可草拟，但最后两项默认要求用户主动确认。

## 7. Knowledge｜跨论文知识页

### 7.1 Topic Overview

- 主题定义与范围；
- 阅读进度；
- Verified 命题数；
- 仍无证据的问题；
- 方法谱系；
- 近期变化。

### 7.2 Proposition Matrix

行是规范化命题，列是论文；单元格为：支持、反对、限定、不可比、未涉及。

点击单元格展开：

- 该论文对应 Claim；
- Anchor；
- 实验设置；
- 用户判断；
- “为何不可比”的字段差异。

颜色只做辅助，必须有符号与文字。

### 7.3 Research Gap

AI 可以提出缺口，但必须区分：

- 证据中直接暴露的缺口；
- 覆盖不足导致的候选缺口；
- AI 推断的研究机会。

用户可将缺口转为新 Topic、阅读任务或 Git issue（后续能力）。

## 8. Library｜文献库

默认以高密度表/列表为主，不做卡片墙。

列：状态、标题、主题角色、Zotero、PDF、待审阅、最后阅读、同步。支持：

- Collection/Tag；
- 已排队/阅读中/已验证/已综合；
- 有无合法 PDF；
- Anchor 失效；
- 待同步/冲突；
- 批量重新解析、导出、重新匹配。

## 9. Sync｜同步中心

### 9.1 Overview

三个独立状态：

- Zotero；
- 本地 Git；
- GitHub Remote。

每个显示：最近成功、待处理、错误、冲突和明确动作。

### 9.2 Preview-first

第一次写入或有冲突时展示计划：

- 将创建/修改哪些 Zotero Item、Attachment、Note；
- 将创建/修改哪些文件；
- Commit message；
- 是否 push；
- 不会被修改的用户内容。

### 9.3 冲突界面

不得只给“保留本地/远端”二选一。对于 Markdown 管理区块，展示：

- 领域对象版本；
- 当前 Git 文件差异；
- PaperWeave 管理区；
- 用户手写区；
- 逐块合并和重新渲染选项。

## 10. Settings｜设置

分区：

- Workspace；
- Models；
- Zotero；
- Git/GitHub；
- PDF & Parsing；
- Privacy & Cloud；
- Appearance & Language；
- Shortcuts；
- Diagnostics。

模型设置必须显示：任务路由、能力检测、上下文上限、估算成本和隐私警示。API Key 输入后不可再次明文显示，只能替换/删除。

## 11. 关键快捷键

| 快捷键 | 动作 |
|---|---|
| `Cmd/Ctrl + K` | 全局命令面板 |
| `Cmd/Ctrl + O` | 打开/导入 PDF |
| `Cmd/Ctrl + F` | 当前论文搜索 |
| `Cmd/Ctrl + Shift + F` | 全局知识搜索 |
| `H` | 对选区高亮 |
| `C` | 创建 Claim draft |
| `N` | 创建用户笔记 |
| `A` / `E` / `R` | 审阅卡接受/编辑/驳回（聚焦时） |
| `[` / `]` | 上一/下一待审阅项 |
| `Cmd/Ctrl + S` | 本地保存；不等于外部同步 |
| `Cmd/Ctrl + Shift + S` | 打开同步预览 |

快捷键可编辑，输入框聚焦时避免冲突。

## 12. 状态与反馈

### 12.1 状态文案

区分：

- 本地已保存；
- 等待 Zotero；
- 已写入 Zotero，等待 Zotero 自身云同步；
- 已渲染 Git 文件；
- 已本地 commit；
- 已 push GitHub；
- 冲突/失败。

禁止用一个模糊“已同步”覆盖不同层级。

### 12.2 生成中

流式生成时卡片进入 skeleton → partial → validating → draft。结构校验失败不得把半成品当成可接受 Claim。

### 12.3 撤销

- 接受/驳回/移动角色/删除有本地 Undo；
- 已执行外部写入时 Undo 创建反向变更，而不是假装从未发生；
- destructive 操作显示目标系统和影响。

## 13. 无障碍

- 完整键盘导航与可见焦点；
- 所有状态不只依赖颜色；
- PDF 文本层支持屏幕阅读器，扫描件给出 OCR 状态；
- 可调整字体、行距和对比；
- 动效遵循 reduced motion；
- 右侧 AI 面板可放大到主区；
- 锚点跳转后焦点移动到对应原文；
- 表格/命题矩阵提供线性可访问视图。

## 14. 响应式策略

产品以 13–16 英寸桌面为主：

- ≥1440 px：三栏完整；
- 1100–1439 px：左栏默认收起，右栏 360–420 px；
- 800–1099 px：PDF 与工作区双栏切换；
- <800 px：只提供阅读/查看/轻量笔记，不承诺完整主题编排。

首版不将手机 Web 体验作为发布门槛。

## 15. 视觉方向建议

关键词：学术编辑部、冷静、证据感、长时间阅读友好。

- 中性浅灰/纸色背景，但 PDF 页面保持真实白；
- 墨色正文，低饱和蓝为交互主色；
- Foundation/Frontier/Counterpoint 用金/蓝/红作语义辅助；
- 避免荧光 AI 渐变、聊天气泡泛滥和大面积玻璃拟态；
- 控件克制，信息密度高于消费级笔记，但通过留白和排版控制疲劳；
- Claim 卡的 provenance、状态和 Anchor 是视觉层级核心。

`prototype/index.html` 验证信息结构和关键状态，不应被视为最终视觉定稿。
